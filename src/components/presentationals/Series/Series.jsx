import React, { Component } from 'react';

class Series extends Component {
  render() {
    console.log('Series', this.props)
    // const seriesItem = get(series, 'items', []);
    return (
      <div>

      </div>
    );
  }
}

export default Series;
